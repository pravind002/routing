import 'package:flutter/material.dart';

class AdminDesignation extends StatelessWidget {
  const AdminDesignation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('My Designation'),
    );
  }
}
